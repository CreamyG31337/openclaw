# Cloned openclaw upstream + our patches; push this to your fork, don't commit it here
openclaw-src/

# Sensitive deploy config (server, tokens, keys) — copy .env.example to .env and fill in
deploy/.env
