# Copy this file to .env and fill in your values. .env is gitignored.
# Optional: leave a line commented or empty to use script defaults.

# Server and SSH (required for deploy)
OPENCLAW_SERVER=your-server
OPENCLAW_DEPLOY_USER=deploy
OPENCLAW_KEY_PATH=C:\path\to\your\private_key

# Repo to clone on server (default: openclaw/openclaw). Set to your fork to deploy your branch (e.g. models dropdown).
# OPENCLAW_REPO=https://github.com/YOUR_USER/openclaw.git
# OPENCLAW_REPO_BRANCH=deploy
# Optional fixed gateway token (if set, deploy always keeps this token)
# OPENCLAW_GATEWAY_TOKEN=

# Local sync automation (optional, used by run-sync-and-deploy.cmd)
# Local path to your clean repo clone (relative to deploy/ or absolute path)
# OPENCLAW_LOCAL_REPO_DIR=..\openclaw-clean
# Remote names in that local repo
# OPENCLAW_UPSTREAM_REMOTE_NAME=origin
# OPENCLAW_UPSTREAM_BRANCH=main
# OPENCLAW_FORK_REMOTE_NAME=fork
# If local working tree has uncommitted/untracked changes, auto-commit them before rebase
# OPENCLAW_AUTO_COMMIT_DIRTY=1

# GitHub Container Registry — for deploy-with-registry.ps1 (push image to ghcr.io)
# Token needs write:packages. Create at https://github.com/settings/tokens
# GHCR_TOKEN=ghp_xxxx

# Z.AI (optional) — default model key; override or leave unset to use script default
# ZAI_API_KEY=

# OpenAI Codex CLI (optional) — for headless codex exec in the gateway; also add to server .env when using deploy
# OPENAI_API_KEY=

# OpenRouter (optional) — unified LLM routing (Claude, GPT-4, Gemini, etc. via one key). See deploy/OPENROUTER.md.
# OPENROUTER_API_KEY=
